{
  "manifest_version": 2,
  "name": "Ispy",
  "description": "Takes a pciture through the webcam and displays it in the newtab.",
  "version": "1",
  "background": { // runs in the background all the time
  	"scripts": ["index.js"],
  "persistent": false
  },
  "permissions": ["activeTab", "storage"]
}